2021.11.17
项目中用到了两种方法去编译so
第一种方式是ndk手动编译c文件，手动执行命令生成so，现在已经不使用了。
第二种方式是使用cmake的方法，让studio自动生成so，这个是当前用的最多的。

对于截图中说的使用javah命令生成c文件的方式，并不是一定要经过这个步骤，只要最终能够生成正确的c文件就可以。
有了c文件，执行命令或者让studio自动打包即可。

然后，现在studio在新建项目的时候，可以选择创建c++项目，这样在生成的项目中，会自动生成c++相关的目录，方便快捷。